name: test_project
description: "A new Flutter project."
# publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.5.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  speech_to_text: ^7.0.0
  google_generative_ai: ^0.4.6
  firebase_core: any
  firebase_auth: any
  firebase_storage: any
  cloud_firestore: any
  image_picker: ^1.1.2
  camera: ^0.11.0
  flutter_image_compress: ^2.1.0
  path_provider: ^2.0.15
  tflite_flutter: ^0.11.0
  image: ^4.5.2
  google_fonts: ^6.2.1
  fluttertoast: ^8.2.10

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  assets:
    - assets/models/fruits_model_optimized.tflite
  uses-material-design: true
